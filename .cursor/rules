# Total Vue AI Coding Rules

- 使用 Vue3 + TypeScript + Composition API。
- 页面展示、章节说明、按钮文案全部使用中文。
- 新知识点必须按 `K_序号` 命名，并在左侧导航可见。
- 每个案例必须包含演示、关键代码、原理、处理流程、注意事项、解决的问题。
- 不要新增重复知识点；如果概念相近，扩展原案例而不是新建章节。
- 不要把业务状态随意放到 Pinia；只有跨组件或跨页面共享才使用 store。
- 修改后运行 `pnpm type-check`、`pnpm test`、`pnpm build`。
